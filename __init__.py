"""stapel-vocabularies — reference vocabularies for the Stapel framework.

Large catalogues (phone models, car generations, city districts) that cannot
be inlined into a category's feature schema: levels + terms + edges as a DAG,
a public typeahead/cascade read surface, a loader for reviewed fixtures, and
the two ``VocabularyResolver`` implementations stapel-attributes' ``ref_select``
/ ``ref_hierarchical_select`` types resolve values through.
"""
